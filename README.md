Dit is een server die op poort 8080 luistert.

Ik moet telkens httpd.exe uitgooien, misschien die service ff niet automatisch laten starten

Als ik een CMD met elevated priviliges heb kan ik netstat -b draaien om te zien welk proces welke poort gebruikt.